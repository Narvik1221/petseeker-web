import stylex from "@stylexjs/stylex";
const styles = stylex.create({
  cardListContainer: {
    display:"flex",
    width:"100%",
    flexWrap:"wrap",
    gap:"20px",
  },
});

export default styles;
